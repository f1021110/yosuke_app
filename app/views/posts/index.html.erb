<br>
  <% @posts.each do |post| %>
      <div class="row">
        <div class="">
          <div class="">
            <h2 class="">
              <%= post.user.name %>
            </h2>
            <%= image_tag post.image.url %>
            <%= image_tag '', class: '' %>
            <%= image_tag '', class: '' %>
            <p class="">
              <%= post.description %>
            </p>
          </div>
        </div>
      </div>
    <% end %>